import { h } from 'vue'
import type { VNode } from 'vue'
import { Icon, IconTypes } from '@/components/Icon'

export const useIcon = (props: IconTypes): VNode => {
  let iEl = h(Icon, props)
  // console.log(iEl, 'iEl')
  return iEl
}
